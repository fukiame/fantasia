From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Sat, 1 Aug 2026 15:42:50 +0700
Subject: [PATCH] lib: onReorder -> onReorderItem

Signed-off-by: fukiame <fukiame@proton.me>

diff --git a/lib/features/playlists/screens/playlist_detail_screen.dart b/lib/features/playlists/screens/playlist_detail_screen.dart
index 27edaf4c0..8958fbd69 100644
--- a/lib/features/playlists/screens/playlist_detail_screen.dart
+++ b/lib/features/playlists/screens/playlist_detail_screen.dart
@@ -458,7 +458,7 @@ class _PlaylistDetailScreenState extends ConsumerState<PlaylistDetailScreen>
                                 child: child,
                               );
                             },
-                            onReorder: (oldIndex, newIndex) {
+                            onReorderItem: (oldIndex, newIndex) {
                               if (_sortOption != PlaylistSortOption.manual) {
                                 return;
                               }
@@ -470,10 +470,8 @@ class _PlaylistDetailScreenState extends ConsumerState<PlaylistDetailScreen>
                                     newIndex,
                                   );
                               setState(() {
-                                var adjustedNew = newIndex;
-                                if (oldIndex < adjustedNew) adjustedNew -= 1;
                                 final item = _songs.removeAt(oldIndex);
-                                _songs.insert(adjustedNew, item);
+                                _songs.insert(newIndex, item);
                               });
                             },
                             itemBuilder: (context, index) {
diff --git a/lib/features/queue/screens/queue_screen.dart b/lib/features/queue/screens/queue_screen.dart
index 036c0bac4..f3171935c 100644
--- a/lib/features/queue/screens/queue_screen.dart
+++ b/lib/features/queue/screens/queue_screen.dart
@@ -177,13 +177,10 @@ class _QueueScreenState extends ConsumerState<QueueScreen> {
                                   )
                                 : SliverReorderableList(
                                     itemCount: upNext.length,
-                                    onReorder: (oldIndex, newIndex) async {
-                                      final targetIndex = newIndex > oldIndex
-                                          ? newIndex - 1
-                                          : newIndex;
+                                    onReorderItem: (oldIndex, newIndex) async {
                                       await ref
                                           .read(playerProvider.notifier)
-                                          .moveUpNextItem(oldIndex, targetIndex);
+                                          .moveUpNextItem(oldIndex, newIndex);
                                     },
                                     itemBuilder: (context, index) {
                                       final song = upNext[index];
@@ -264,13 +261,10 @@ class _QueueScreenState extends ConsumerState<QueueScreen> {
                                   )
                                 : SliverReorderableList(
                                     itemCount: queue.length,
-                                    onReorder: (oldIndex, newIndex) async {
-                                      final targetIndex = newIndex > oldIndex
-                                          ? newIndex - 1
-                                          : newIndex;
+                                    onReorderItem: (oldIndex, newIndex) async {
                                       await ref
                                           .read(playerProvider.notifier)
-                                          .moveQueueItem(oldIndex, targetIndex);
+                                          .moveQueueItem(oldIndex, newIndex);
                                     },
                                     itemBuilder: (context, index) {
                                       final song = queue[index];
diff --git a/lib/features/settings/screens/bottom_bar_settings_screen.dart b/lib/features/settings/screens/bottom_bar_settings_screen.dart
index 5efce7a95..1e61461d2 100644
--- a/lib/features/settings/screens/bottom_bar_settings_screen.dart
+++ b/lib/features/settings/screens/bottom_bar_settings_screen.dart
@@ -99,7 +99,7 @@ class BottomBarSettingsScreen extends ConsumerWidget {
                 );
               },
               itemCount: enabled.length,
-              onReorder: (oldIndex, newIndex) {
+              onReorderItem: (oldIndex, newIndex) {
                 ref
                     .read(navBarConfigProvider.notifier)
                     .reorderButtons(oldIndex, newIndex);
diff --git a/lib/models/nav_bar_config.dart b/lib/models/nav_bar_config.dart
index 975f1ef62..6e851430d 100644
--- a/lib/models/nav_bar_config.dart
+++ b/lib/models/nav_bar_config.dart
@@ -47,7 +47,6 @@ class NavBarConfig {
 
   NavBarConfig reorder(int oldIndex, int newIndex) {
     final list = List<NavBarButton>.from(enabledButtons);
-    if (oldIndex < newIndex) newIndex -= 1;
     final item = list.removeAt(oldIndex);
     list.insert(newIndex, item);
     return copyWith(enabledButtons: list);
diff --git a/lib/services/playlist_service.dart b/lib/services/playlist_service.dart
index b095acd4d..682ffa0e2 100644
--- a/lib/services/playlist_service.dart
+++ b/lib/services/playlist_service.dart
@@ -194,10 +194,8 @@ class PlaylistService {
 
     final playlist = _playlists[index];
     final songIds = List<String>.from(playlist.songIds);
-    var adjustedNew = newIndex;
-    if (oldIndex < adjustedNew) adjustedNew -= 1;
     final item = songIds.removeAt(oldIndex);
-    songIds.insert(adjustedNew, item);
+    songIds.insert(newIndex, item);
 
     final updated = playlist.copyWith(
       songIds: songIds,
-- 
2.55.0

