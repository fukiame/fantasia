From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Mon, 10 Aug 2026 08:14:47 +0700
Subject: [PATCH] leftist

Signed-off-by: fukiame <fukiame@proton.me>

diff --git a/lib/features/player/widgets/animated_song_scene.dart b/lib/features/player/widgets/animated_song_scene.dart
index 24c6f01c..535296de 100644
--- a/lib/features/player/widgets/animated_song_scene.dart
+++ b/lib/features/player/widgets/animated_song_scene.dart
@@ -525,11 +525,13 @@ class AnimatedSongScene extends StatelessWidget {
     final hasAlbum = song.album != null && song.album!.trim().isNotEmpty;
 
     return Column(
+      crossAxisAlignment: CrossAxisAlignment.start,
       children: [
+        Row( children: [ Expanded( child: const SizedBox.shrink(), ), ], ),
         if (artworkCardShowTitle)
           Text(
             song.title,
-            textAlign: TextAlign.center,
+            textAlign: TextAlign.left,
             maxLines: 2,
             overflow: TextOverflow.ellipsis,
             style: TextStyle(
@@ -547,7 +549,7 @@ class AnimatedSongScene extends StatelessWidget {
             onTap: () => onNavigateToArtistDetail(song),
             child: Text(
               song.artist,
-              textAlign: TextAlign.center,
+              textAlign: TextAlign.left,
               maxLines: 1,
               overflow: TextOverflow.ellipsis,
               style: TextStyle(
@@ -564,11 +566,6 @@ class AnimatedSongScene extends StatelessWidget {
           SizedBox(height: artistToAlbumSpacing),
             GestureDetector(
               onTap: () => onNavigateToAlbumDetail(song),
-              child: Container(
-                padding: EdgeInsets.symmetric(
-                  horizontal: albumHorizontalPadding,
-                  vertical: albumVerticalPadding,
-                ),
                 child: Text(
                   song.album!,
                   maxLines: 1,
@@ -579,7 +576,6 @@ class AnimatedSongScene extends StatelessWidget {
                     color: Colors.white.withValues(alpha: 0.68),
                   ),
                 ),
-              ),
             ),
         ],
       ],
diff --git a/lib/features/player/widgets/player_action_button_row.dart b/lib/features/player/widgets/player_action_button_row.dart
index b8536e55..11480009 100644
--- a/lib/features/player/widgets/player_action_button_row.dart
+++ b/lib/features/player/widgets/player_action_button_row.dart
@@ -83,20 +83,6 @@ class _PlayerActionButtonRowState extends ConsumerState<PlayerActionButtonRow> {
       mainAxisAlignment: MainAxisAlignment.spaceBetween,
       crossAxisAlignment: CrossAxisAlignment.center,
       children: [
-        _buildSideButtons(
-          topAction: leftTopAction,
-          bottomAction: leftAction,
-          context: context,
-          song: song,
-          actionPadding: actionPadding,
-          actionRadius: actionRadius,
-          actionIconSize: actionIconSize,
-          inactiveBg: inactiveBg,
-          inactiveBorder: inactiveBorder,
-          albumColor: albumColor,
-          accentBlend: accentBlend,
-          hasAlbumTint: hasAlbumTint,
-        ),
         Flexible(
           child: Row(
             mainAxisSize: MainAxisSize.min,
@@ -150,6 +136,22 @@ class _PlayerActionButtonRowState extends ConsumerState<PlayerActionButtonRow> {
             ],
           ),
         ),
+	Row( children: [
+        _buildSideButtons(
+          topAction: leftTopAction,
+          bottomAction: leftAction,
+          context: context,
+          song: song,
+          actionPadding: actionPadding,
+          actionRadius: actionRadius,
+          actionIconSize: actionIconSize,
+          inactiveBg: inactiveBg,
+          inactiveBorder: inactiveBorder,
+          albumColor: albumColor,
+          accentBlend: accentBlend,
+          hasAlbumTint: hasAlbumTint,
+        ),
+	const SizedBox(width: 2.0),
         _buildSideButtons(
           topAction: rightTopAction,
           bottomAction: rightAction,
@@ -164,6 +166,7 @@ class _PlayerActionButtonRowState extends ConsumerState<PlayerActionButtonRow> {
           accentBlend: accentBlend,
           hasAlbumTint: hasAlbumTint,
         ),
+	], ),
       ],
     );
   }
-- 
2.55.0

