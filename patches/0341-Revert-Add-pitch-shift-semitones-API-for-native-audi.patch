From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Thu, 30 Jul 2026 18:06:22 +0700
Subject: [PATCH] Revert "Add pitch shift semitones API for native audio
 engine"

This reverts commit a483360e2aa61bd8e6db34a28c3c0464045b1850.

diff --git a/lib/src/rust/api/audio_api.dart b/lib/src/rust/api/audio_api.dart
index fab05d48..81d1ec7e 100644
--- a/lib/src/rust/api/audio_api.dart
+++ b/lib/src/rust/api/audio_api.dart
@@ -209,13 +209,6 @@ Future<void> audioSetEqualizer({
   gainsDb: gainsDb,
 );
 
-/// Set pitch shift in semitones for the native audio engine (tempo preserved).
-/// 0 = bypass. Range is clamped internally to ±12 semitones.
-Future<void> audioSetPitchShiftSemitones({required double semitones}) => RustLib
-    .instance
-    .api
-    .crateApiAudioApiAudioSetPitchShiftSemitones(semitones: semitones);
-
 /// Configure compressor settings for the native audio engine.
 Future<void> audioSetCompressor({
   required bool enabled,
-- 
2.55.0

