From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Fri, 31 Jul 2026 21:05:54 +0700
Subject: [PATCH] Revert "Reformat apply_tag_fields function signature"

This reverts commit 50d92c12334cbe6c6cce241d317c858926d0bcec.

diff --git a/rust/src/api/metadata_editor.rs b/rust/src/api/metadata_editor.rs
index b3f8f2fce..4440bcec9 100644
--- a/rust/src/api/metadata_editor.rs
+++ b/rust/src/api/metadata_editor.rs
@@ -162,10 +162,7 @@ pub fn write_tags_to_temp(
     })
 }
 
-fn apply_tag_fields(
-    tagged_file: &mut lofty::file::TaggedFile,
-    fields: &TagEditFields,
-) -> Result<(), String> {
+fn apply_tag_fields(tagged_file: &mut lofty::file::TaggedFile, fields: &TagEditFields) -> Result<(), String> {
     let tag = tagged_file.primary_tag_mut();
     let tag = match tag {
         Some(t) => Some(t),
@@ -202,4 +199,4 @@ fn apply_tag_fields(
         }
         None => Err("No tag found in file".to_string()),
     }
-}
+}
\ No newline at end of file
-- 
2.55.0

