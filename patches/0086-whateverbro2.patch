From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Tue, 28 Jul 2026 13:27:49 +0700
Subject: [PATCH] whateverbro2

Signed-off-by: fukiame <fukiame@proton.me>

diff --git a/lib/app/app.dart b/lib/app/app.dart
index 7eff1b9b..a3ac44e4 100644
--- a/lib/app/app.dart
+++ b/lib/app/app.dart
@@ -754,6 +754,7 @@ class _MainShellState extends ConsumerState<MainShell>
         showMiniPlayer: true,
         miniPlayerWidget: _EmbeddedMiniPlayer(
             collapsed: _isBottomBarCollapsed,
+	    isNull: false,
         ),
     );
   }
@@ -762,8 +763,9 @@ class _MainShellState extends ConsumerState<MainShell>
 /// Embedded mini player widget that uses Riverpod for state.
 class _EmbeddedMiniPlayer extends ConsumerStatefulWidget {
   final bool collapsed;
+  final bool isNull;
 
-  const _EmbeddedMiniPlayer({this.collapsed = false});
+  const _EmbeddedMiniPlayer({this.collapsed = false, this.isNull = false});
 
   @override
   ConsumerState<_EmbeddedMiniPlayer> createState() =>
@@ -937,9 +939,11 @@ class _EmbeddedMiniPlayerState extends ConsumerState<_EmbeddedMiniPlayer> {
     final currentSong = ref.watch(currentSongProvider);
 
     if (currentSong == null) {
+      isNull = true;
       return const Offstage(offstage: true);
     }
 
+    isNull = false;
     return GestureDetector(
       behavior: HitTestBehavior.opaque,
       onTap: () async {
diff --git a/lib/widgets/navigation/flick_nav_bar.dart b/lib/widgets/navigation/flick_nav_bar.dart
index 2cefbb11..4a37daeb 100644
--- a/lib/widgets/navigation/flick_nav_bar.dart
+++ b/lib/widgets/navigation/flick_nav_bar.dart
@@ -41,7 +41,7 @@ class _FlickNavBarState extends State<FlickNavBar> {
     final borderRadius = BorderRadius.circular(context.scaleSize(20));
 
     bool sw = widget.showMiniPlayer && widget.miniPlayerWidget != null
-                && !(widget.miniPlayerWidget is Offstage);
+                && !widget.miniPlayerWidget.isNull;
     bool bl = buttons.length > 1;
 
     return AnimatedContainer(
@@ -128,6 +128,18 @@ class _FlickNavBarState extends State<FlickNavBar> {
                             letterSpacing: 0.4,
                           ),
                         ),
+                        Text(
+                          widget.miniPlayerWidget.isNull.toString(),
+                          maxLines: 1,
+                          overflow: TextOverflow.ellipsis,
+                          style: TextStyle(
+                            fontFamily: 'ProductSans',
+                            fontSize: 14,
+                            fontWeight: FontWeight.w400,
+                            color: Colors.white,
+                            letterSpacing: 0.4,
+                          ),
+                        ),
           if (bl || !sw)
           AnimatedCrossFade(
             firstChild: _buildNavigationRow(context, buttons),
-- 
2.55.0

