From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Sun, 2 Aug 2026 19:33:50 +0700
Subject: [PATCH] Revert "Conditionally show USB volume control based on
 preference"

This reverts commit bd883a6079100d2d47632a0c62265dfc86be976e.

diff --git a/lib/features/settings/screens/uac2_settings_screen.dart b/lib/features/settings/screens/uac2_settings_screen.dart
index 559a54695..326939d5b 100644
--- a/lib/features/settings/screens/uac2_settings_screen.dart
+++ b/lib/features/settings/screens/uac2_settings_screen.dart
@@ -30,7 +30,6 @@ class _Uac2SettingsScreenState extends ConsumerState<Uac2SettingsScreen> {
     final selectedDevice = ref.watch(selectedUac2DeviceProvider);
     final deviceStatus = ref.watch(uac2DeviceStatusProvider);
     final currentSong = ref.watch(currentSongProvider);
-    final appPreferences = ref.watch(appPreferencesProvider);
 
     return DisplayModeWrapper(
       child: Scaffold(
@@ -89,8 +88,7 @@ class _Uac2SettingsScreenState extends ConsumerState<Uac2SettingsScreen> {
                             ],
                             if (deviceStatus != null &&
                                 deviceStatus.state != Uac2State.idle &&
-                                deviceStatus.hasVolumeControl &&
-                                appPreferences.showUsbVolumeOnSettings) ...[
+                                deviceStatus.hasVolumeControl) ...[
                               const SizedBox(height: AppConstants.spacingLg),
                               _buildSectionHeader(context, 'Volume Control'),
                               const Uac2VolumeControl(),
-- 
2.55.0

