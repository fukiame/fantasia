From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Wed, 29 Jul 2026 14:11:58 +0700
Subject: [PATCH] lib: no haptics

Signed-off-by: fukiame <fukiame@proton.me>

diff --git a/lib/app/app.dart b/lib/app/app.dart
index fd85331a..4dba0147 100644
--- a/lib/app/app.dart
+++ b/lib/app/app.dart
@@ -20,7 +20,6 @@ import 'package:flick/features/playlists/screens/playlists_screen.dart';
 import 'package:flick/features/favorites/screens/favorites_screen.dart';
 import 'package:flick/features/search/screens/search_screen.dart';
 import 'package:flick/core/utils/navigation_helper.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/core/constants/app_constants.dart';
 import 'package:flick/features/player/widgets/ambient_background.dart';
 import 'package:flick/widgets/navigation/flick_nav_bar.dart';
@@ -91,7 +90,6 @@ class _MainShellState extends ConsumerState<MainShell>
   // Cleared once the target position is reached.
   int? _programmaticPageTarget;
 
-  int _lastHapticPage = -1;
   late final PlayerService _playerService;
 
   DateTime? _lastBackPressTime;
@@ -122,7 +120,6 @@ class _MainShellState extends ConsumerState<MainShell>
     _pageController = PageController(
       initialPage: initialPosition >= 0 ? initialPosition : 0,
     );
-    _lastHapticPage = initialPosition >= 0 ? initialPosition : 0;
     _pageController.addListener(_onPageScroll);
     _navBarAnimationController = AnimationController(
       vsync: this,
@@ -207,7 +204,6 @@ class _MainShellState extends ConsumerState<MainShell>
       if (newPosition < 0) {
         // Current page was removed, jump to first page in new order
         if (_pageController.hasClients && newOrder.isNotEmpty) {
-          _lastHapticPage = 0;
           _pageController.jumpToPage(0);
           ref
               .read(navigationIndexProvider.notifier)
@@ -217,7 +213,6 @@ class _MainShellState extends ConsumerState<MainShell>
       }
 
       if (oldPosition != newPosition && _pageController.hasClients) {
-        _lastHapticPage = newPosition;
         _pageController.jumpToPage(newPosition);
         ref.read(navigationIndexProvider.notifier).setIndex(currentPageIndex);
       }
@@ -248,7 +243,6 @@ class _MainShellState extends ConsumerState<MainShell>
           }
 
           _programmaticPageTarget = position;
-          _lastHapticPage = position;
           if (AppConstants.animationNormal == Duration.zero) {
             _pageController.jumpToPage(position);
           } else {
@@ -397,10 +391,6 @@ class _MainShellState extends ConsumerState<MainShell>
     final page = _pageController.page;
     if (page == null) return;
     final rounded = page.round();
-    if (rounded != _lastHapticPage && (page - rounded).abs() <= 0.35) {
-      _lastHapticPage = rounded;
-      AppHaptics.selection();
-    }
   }
 
   void _startIdleTimer() {
@@ -582,11 +572,6 @@ class _MainShellState extends ConsumerState<MainShell>
                     final currentConfig = ref.read(navBarConfigProvider);
                     final currentOrder = _getPageOrder(currentConfig);
                     if (position < 0 || position >= currentOrder.length) return;
-                    if (_programmaticPageTarget == null &&
-                        position != _lastHapticPage) {
-                      AppHaptics.selection();
-                    }
-                    _lastHapticPage = position;
                     final enabledCount = currentConfig.orderedButtons.length;
                     if (position >= enabledCount) {
                       if (_programmaticPageTarget == position) {
@@ -1018,9 +1003,8 @@ class _RootRouter extends ConsumerWidget {
   Widget build(BuildContext context, WidgetRef ref) {
     final appPreferences = ref.watch(appPreferencesProvider);
 
-    // Apply animation and haptic preferences globally.
+    // Apply animation preferences globally.
     AppConstants.setAnimationsEnabled(appPreferences.animationsEnabled);
-    AppHaptics.setEnabled(appPreferences.hapticsEnabled);
 
     final child = const MainShell();
 
diff --git a/lib/core/utils/app_haptics.dart b/lib/core/utils/app_haptics.dart
deleted file mode 100644
index 23656aa6..00000000
--- a/lib/core/utils/app_haptics.dart
+++ /dev/null
@@ -1,48 +0,0 @@
-import 'package:flutter/services.dart';
-
-/// Centralized haptic patterns used across Flick interactions.
-class AppHaptics {
-  AppHaptics._();
-
-  static bool _enabled = true;
-
-  static void setEnabled(bool enabled) {
-    _enabled = enabled;
-  }
-
-  /// Wraps a callback so it fires [tap] haptic before executing.
-  static VoidCallback wrap(VoidCallback callback) {
-    return () {
-      tap();
-      callback();
-    };
-  }
-
-  /// Wraps an async callback so it fires [tap] haptic before executing.
-  static Future<void> Function() wrapAsync(Future<void> Function() callback) {
-    return () async {
-      tap();
-      await callback();
-    };
-  }
-
-  static void tap() {
-    if (!_enabled) return;
-    HapticFeedback.lightImpact();
-  }
-
-  static void selection() {
-    if (!_enabled) return;
-    HapticFeedback.selectionClick();
-  }
-
-  static void confirm() {
-    if (!_enabled) return;
-    HapticFeedback.mediumImpact();
-  }
-
-  static void emphasis() {
-    if (!_enabled) return;
-    HapticFeedback.heavyImpact();
-  }
-}
diff --git a/lib/features/player/widgets/album_art_box.dart b/lib/features/player/widgets/album_art_box.dart
index dfde8a81..8204eff3 100644
--- a/lib/features/player/widgets/album_art_box.dart
+++ b/lib/features/player/widgets/album_art_box.dart
@@ -4,7 +4,6 @@ import 'package:flutter/material.dart';
 import 'package:flutter/gestures.dart';
 import 'package:flick/services/player_service.dart';
 import 'package:flick/models/song.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/core/utils/responsive.dart';
 import 'package:lucide_icons_flutter/lucide_icons.dart';
 import 'package:flick/widgets/common/cached_image_widget.dart';
@@ -52,8 +51,6 @@ class _AlbumArtBoxState extends State<AlbumArtBox>
   Duration _lastObservedPosition = Duration.zero;
   final ValueNotifier<double> _userRotationOffset = ValueNotifier(0.0);
   bool _isUserDragging = false;
-  double _rotationHapticAccumulator = 0.0;
-  static const double _hapticTickInterval = math.pi / 16;
   late final DoubleTapGestureRecognizer _doubleTapRecognizer;
   late final TapGestureRecognizer _singleTapRecognizer;
   late final _RotationSeekRecognizer _rotationRecognizer;
@@ -220,13 +217,11 @@ class _AlbumArtBoxState extends State<AlbumArtBox>
     final service = widget.playerService;
     if (service == null) return;
     _isUserDragging = true;
-    _rotationHapticAccumulator = 0.0;
     service.beginInteractiveSeek();
     _spinController.stop();
     _seekAngleController.stop();
     _seekAngleController.value = 0;
     _lastObservedPosition = service.positionNotifier.value;
-    AppHaptics.confirm();
   }
 
   void _onRotationUpdate(double delta) {
@@ -235,12 +230,6 @@ class _AlbumArtBoxState extends State<AlbumArtBox>
     if (service == null) return;
     _userRotationOffset.value += delta;
 
-    _rotationHapticAccumulator += delta.abs();
-    if (_rotationHapticAccumulator >= _hapticTickInterval) {
-      _rotationHapticAccumulator -= _hapticTickInterval;
-      AppHaptics.tap();
-    }
-
     final msPerRadian = (_secondsPerVinylRotation * 1000) / (2 * math.pi);
     final seekMsDelta = (delta * msPerRadian).round();
     if (seekMsDelta != 0) {
@@ -262,11 +251,9 @@ class _AlbumArtBoxState extends State<AlbumArtBox>
   void _onRotationEnd() {
     _isUserDragging = false;
     widget.playerService?.endInteractiveSeek();
-    _rotationHapticAccumulator = 0.0;
     if (_isVinyl && _isPlaying) {
       _spinController.repeat();
     }
-    AppHaptics.confirm();
   }
 
   void _animateSeek(int deltaMs) {
@@ -288,7 +275,6 @@ class _AlbumArtBoxState extends State<AlbumArtBox>
   }
 
   void _toggle() {
-    AppHaptics.confirm();
     setState(() {
       _isVinyl = !_isVinyl;
       if (_isVinyl) {
@@ -309,7 +295,6 @@ class _AlbumArtBoxState extends State<AlbumArtBox>
   void _handleVinylSingleTap() {
     if (!_isVinyl) return;
     if (_outlineController.isAnimating) return;
-    AppHaptics.confirm();
     if (_isRotationEnabled) {
       _outlineController.reverse();
     } else {
diff --git a/lib/features/player/widgets/animated_song_scene.dart b/lib/features/player/widgets/animated_song_scene.dart
index 75440147..2a0c4512 100644
--- a/lib/features/player/widgets/animated_song_scene.dart
+++ b/lib/features/player/widgets/animated_song_scene.dart
@@ -7,7 +7,6 @@ import 'package:flick/widgets/uac2/uac2_error_notification.dart';
 import 'package:lucide_icons_flutter/lucide_icons.dart';
 import 'package:flick/core/theme/app_colors.dart';
 import 'package:flick/core/constants/app_constants.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/core/utils/responsive.dart';
 import 'package:flick/models/song.dart';
 import 'package:flick/models/album_color_mode.dart';
@@ -574,7 +573,6 @@ class AnimatedSongScene extends StatelessWidget {
         shape: BoxShape.circle,
       ),
       child: IconButton(
-        onPressed: AppHaptics.wrap(onTap),
         padding: EdgeInsets.all(context.responsive(8.0, 10.0, 12.0)),
         constraints: const BoxConstraints(),
         icon: Icon(
diff --git a/lib/features/player/widgets/mini_player.dart b/lib/features/player/widgets/mini_player.dart
index 05f58e11..86e3bbd4 100644
--- a/lib/features/player/widgets/mini_player.dart
+++ b/lib/features/player/widgets/mini_player.dart
@@ -2,7 +2,6 @@ import 'package:flutter/material.dart';
 import 'package:flutter_riverpod/flutter_riverpod.dart';
 import 'package:flick/core/theme/app_colors.dart';
 import 'package:flick/core/constants/app_constants.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/core/utils/navigation_helper.dart';
 import 'package:flick/models/song.dart';
 import 'package:flick/services/player_service.dart';
@@ -29,7 +28,6 @@ class _MiniPlayerState extends ConsumerState<MiniPlayer> {
 
     return GestureDetector(
       onTap: () {
-        AppHaptics.tap();
         _openQueue(context);
       },
       child: AnimatedContainer(
@@ -84,7 +82,6 @@ class _MiniPlayerState extends ConsumerState<MiniPlayer> {
 
         return GestureDetector(
           onTap: () {
-            AppHaptics.tap();
             NavigationHelper.navigateToFullPlayer(
               context,
               heroTag: 'song_art_${song.id}',
@@ -280,7 +277,6 @@ class _MiniPlayerState extends ConsumerState<MiniPlayer> {
                               child: InkWell(
                                 borderRadius: BorderRadius.circular(24),
                                 onTap: () {
-                                  AppHaptics.tap();
                                   _playerService.togglePlayPause();
                                 },
                                 child: Padding(
diff --git a/lib/features/player/widgets/player_controls.dart b/lib/features/player/widgets/player_controls.dart
index dc767bd7..c6561ecb 100644
--- a/lib/features/player/widgets/player_controls.dart
+++ b/lib/features/player/widgets/player_controls.dart
@@ -5,7 +5,6 @@ import 'package:flick/models/song.dart';
 import 'package:flick/models/shuffle_mode.dart';
 import 'package:flick/models/album_color_mode.dart';
 import 'package:flick/core/theme/app_colors.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/core/utils/responsive.dart';
 import 'package:flick/features/player/widgets/album_color_helpers.dart';
 import 'package:flick/features/player/widgets/shuffle_mode_sheet.dart';
@@ -81,11 +80,9 @@ class PlayerControls extends StatelessWidget {
                           };
                           return GestureDetector(
                             onTap: () {
-                              AppHaptics.tap();
                               playerService.toggleShuffle();
                             },
                             onLongPress: () {
-                              AppHaptics.tap();
                               ShuffleModeSheet.show(context, playerService);
                             },
                             child: Container(
@@ -127,7 +124,6 @@ class PlayerControls extends StatelessWidget {
                         ),
                         child: IconButton(
                           onPressed: () {
-                            AppHaptics.tap();
                             onPrevious();
                           },
                           iconSize: context.responsive(18.0, 20.0, 22.0),
@@ -153,7 +149,6 @@ class PlayerControls extends StatelessWidget {
                         ),
                         child: IconButton(
                           onPressed: () {
-                            AppHaptics.tap();
                             onNext();
                           },
                           iconSize: context.responsive(18.0, 20.0, 22.0),
@@ -176,11 +171,9 @@ class PlayerControls extends StatelessWidget {
                               : Colors.white.withValues(alpha: 0.7);
                           return GestureDetector(
                             onTap: () {
-                              AppHaptics.tap();
                               playerService.toggleLoopMode();
                             },
                             onLongPress: () {
-                              AppHaptics.tap();
                               LoopModeSheet.show(context, playerService);
                             },
                             child: Container(
@@ -320,7 +313,6 @@ class _PlayPauseButton extends StatelessWidget {
             ),
             child: IconButton(
               onPressed: () {
-                AppHaptics.tap();
                 playerService.togglePlayPause();
               },
               iconSize: iconSize,
diff --git a/lib/features/settings/screens/bottom_bar_settings_screen.dart b/lib/features/settings/screens/bottom_bar_settings_screen.dart
index d4e15358..783119ae 100644
--- a/lib/features/settings/screens/bottom_bar_settings_screen.dart
+++ b/lib/features/settings/screens/bottom_bar_settings_screen.dart
@@ -4,7 +4,6 @@ import 'package:lucide_icons_flutter/lucide_icons.dart';
 import 'package:flick/core/theme/app_colors.dart';
 import 'package:flick/core/theme/adaptive_color_provider.dart';
 import 'package:flick/core/constants/app_constants.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/models/nav_bar_config.dart';
 import 'package:flick/providers/providers.dart';
 import 'package:flick/features/settings/widgets/settings_widgets.dart';
@@ -284,7 +283,6 @@ class _DisabledNavItem extends StatelessWidget {
       color: Colors.transparent,
       child: InkWell(
         onTap: () {
-          AppHaptics.tap();
           onEnable();
         },
         child: Padding(
diff --git a/lib/features/settings/screens/interface_settings_screen.dart b/lib/features/settings/screens/interface_settings_screen.dart
index 06d30f41..6c9f998a 100644
--- a/lib/features/settings/screens/interface_settings_screen.dart
+++ b/lib/features/settings/screens/interface_settings_screen.dart
@@ -2,7 +2,6 @@ import 'package:flutter/material.dart';
 import 'package:flutter_riverpod/flutter_riverpod.dart';
 import 'package:lucide_icons_flutter/lucide_icons.dart';
 import 'package:flick/core/constants/app_constants.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/providers/providers.dart';
 import 'package:flick/features/settings/widgets/settings_widgets.dart';
 import 'package:flick/features/settings/screens/bottom_bar_settings_screen.dart';
@@ -35,19 +34,6 @@ class InterfaceSettingsScreen extends ConsumerWidget {
                 },
               ),
               const SettingsDivider(),
-              ToggleSetting(
-                icon: LucideIcons.vibrate,
-                title: 'Haptic Feedback',
-                subtitle: 'Enable vibration on interactions',
-                value: appPreferences.hapticsEnabled,
-                onChanged: (value) {
-                  ref
-                      .read(appPreferencesProvider.notifier)
-                      .setHapticsEnabled(value);
-                  AppHaptics.setEnabled(value);
-                },
-              ),
-              const SettingsDivider(),
               ToggleSetting(
                 icon: LucideIcons.arrowLeftRight,
                 title: 'Swipe Actions',
diff --git a/lib/features/settings/screens/logs_screen.dart b/lib/features/settings/screens/logs_screen.dart
index fe469ab1..04a3817e 100644
--- a/lib/features/settings/screens/logs_screen.dart
+++ b/lib/features/settings/screens/logs_screen.dart
@@ -6,7 +6,6 @@ import 'package:share_plus/share_plus.dart';
 import 'package:flick/core/constants/app_constants.dart';
 import 'package:flick/core/theme/app_colors.dart';
 import 'package:flick/core/theme/adaptive_color_provider.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/core/utils/app_log.dart';
 
 class LogsScreen extends ConsumerStatefulWidget {
@@ -139,7 +138,6 @@ class _LogsScreenState extends ConsumerState<LogsScreen> {
         children: [
           IconButton(
             onPressed: () {
-              AppHaptics.tap();
               Navigator.of(context).pop();
             },
             icon: Icon(LucideIcons.chevronLeft, color: context.adaptiveTextPrimary),
@@ -244,7 +242,6 @@ class _LogsScreenState extends ConsumerState<LogsScreen> {
       padding: const EdgeInsets.only(right: AppConstants.spacingXs),
       child: GestureDetector(
         onTap: () {
-          AppHaptics.tap();
           setState(() {
             if (source == null) {
               _sources.clear();
diff --git a/lib/features/settings/screens/settings_screen.dart b/lib/features/settings/screens/settings_screen.dart
index 658e6bbd..6ddcc807 100644
--- a/lib/features/settings/screens/settings_screen.dart
+++ b/lib/features/settings/screens/settings_screen.dart
@@ -117,7 +117,7 @@ class _SettingsScreenState extends State<SettingsScreen> {
                       children: [
                         _CategoryTile(
                           title: 'Interface',
-                          subtitle: 'Animations and haptic feedback',
+                          subtitle: 'Animations',
                           onTap: () => _navigate(
                             context,
                             const InterfaceSettingsScreen(),
diff --git a/lib/features/settings/widgets/settings_widgets.dart b/lib/features/settings/widgets/settings_widgets.dart
index 8703b981..a522308b 100644
--- a/lib/features/settings/widgets/settings_widgets.dart
+++ b/lib/features/settings/widgets/settings_widgets.dart
@@ -4,7 +4,6 @@ import 'package:lucide_icons_flutter/lucide_icons.dart';
 import 'package:flick/core/theme/app_colors.dart';
 import 'package:flick/core/theme/adaptive_color_provider.dart';
 import 'package:flick/core/constants/app_constants.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/core/utils/responsive.dart';
 import 'package:flick/features/player/widgets/ambient_background.dart';
 import 'package:flick/providers/providers.dart';
@@ -118,7 +117,6 @@ class ToggleSetting extends StatelessWidget {
       color: Colors.transparent,
       child: InkWell(
         onTap: () {
-          AppHaptics.tap();
           onChanged(!value);
         },
         child: Padding(
@@ -181,7 +179,6 @@ class NavigationSetting extends StatelessWidget {
       color: Colors.transparent,
       child: InkWell(
         onTap: () {
-          AppHaptics.tap();
           onTap();
         },
         child: Padding(
@@ -248,7 +245,6 @@ class SelectionSetting extends StatelessWidget {
       color: Colors.transparent,
       child: InkWell(
         onTap: () {
-          AppHaptics.tap();
           onTap();
         },
         child: Padding(
@@ -320,7 +316,6 @@ class ActionButton extends StatelessWidget {
       child: InkWell(
         onTap: onTap != null
             ? () {
-                AppHaptics.tap();
                 onTap!();
               }
             : null,
@@ -376,7 +371,6 @@ class CustomSwitch extends StatelessWidget {
   Widget build(BuildContext context) {
     return GestureDetector(
       onTap: () {
-        AppHaptics.selection();
         onChanged(!value);
       },
       child: AnimatedContainer(
@@ -649,7 +643,6 @@ class _SettingsBackHeader extends StatelessWidget {
         children: [
           IconButton(
             onPressed: () {
-              AppHaptics.tap();
               Navigator.of(context).pop();
             },
             icon: Icon(
diff --git a/lib/features/songs/screens/songs_screen.dart b/lib/features/songs/screens/songs_screen.dart
index ede25261..42b19163 100644
--- a/lib/features/songs/screens/songs_screen.dart
+++ b/lib/features/songs/screens/songs_screen.dart
@@ -10,7 +10,6 @@ import 'package:shared_preferences/shared_preferences.dart';
 import 'package:flick/core/theme/app_colors.dart';
 import 'package:flick/core/theme/adaptive_color_provider.dart';
 import 'package:flick/core/constants/app_constants.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/core/utils/responsive.dart';
 import 'package:flick/core/utils/navigation_helper.dart';
 import 'package:flick/models/song.dart';
@@ -2102,7 +2101,6 @@ class _SongListTile extends StatelessWidget {
       child: InkWell(
         borderRadius: BorderRadius.circular(AppConstants.radiusLg),
         onTap: () async {
-          AppHaptics.tap();
           await onTap();
         },
         onLongPress: onLongPress,
@@ -2653,7 +2651,6 @@ class _AlbumListTile extends StatelessWidget {
       child: InkWell(
         borderRadius: BorderRadius.circular(AppConstants.radiusLg),
         onTap: () {
-          AppHaptics.tap();
           onTap();
         },
         child: Container(
diff --git a/lib/features/songs/widgets/song_card.dart b/lib/features/songs/widgets/song_card.dart
index 5e468609..5da0fe4b 100644
--- a/lib/features/songs/widgets/song_card.dart
+++ b/lib/features/songs/widgets/song_card.dart
@@ -3,7 +3,6 @@ import 'dart:async';
 import 'package:flutter/material.dart';
 import 'package:flick/core/theme/app_colors.dart';
 import 'package:flick/core/constants/app_constants.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/models/song.dart';
 import 'package:flick/widgets/common/marquee_widget.dart';
 import 'package:flick/widgets/common/cached_image_widget.dart';
@@ -114,7 +113,6 @@ class _SongCardState extends State<SongCard> {
               (details.primaryVelocity != null &&
                   details.primaryVelocity! < -400);
           if (shouldFavorite) {
-            AppHaptics.confirm();
             setState(() {
               _dragDx = 0;
               _favoriteFlash = true;
@@ -128,7 +126,6 @@ class _SongCardState extends State<SongCard> {
             return;
           }
           if (shouldQueue) {
-            AppHaptics.confirm();
             setState(() {
               _dragDx = 0;
               _queuedFlash = true;
@@ -157,7 +154,6 @@ class _SongCardState extends State<SongCard> {
 
     return GestureDetector(
       onTap: () {
-        AppHaptics.tap();
         widget.onTap?.call();
       },
       onHorizontalDragUpdate: dragUpdate,
diff --git a/lib/providers/app_preferences_provider.dart b/lib/providers/app_preferences_provider.dart
index 8f926541..243c4047 100644
--- a/lib/providers/app_preferences_provider.dart
+++ b/lib/providers/app_preferences_provider.dart
@@ -33,12 +33,6 @@ class AppPreferencesNotifier extends Notifier<AppPreferences> {
     await ref.read(appPreferencesServiceProvider).setAnimationsEnabled(value);
   }
 
-  Future<void> setHapticsEnabled(bool value) async {
-    if (state.hapticsEnabled == value) return;
-    state = state.copyWith(hapticsEnabled: value);
-    await ref.read(appPreferencesServiceProvider).setHapticsEnabled(value);
-  }
-
   Future<void> setShowPlaylistPreviews(bool value) async {
     if (state.showPlaylistPreviews == value) return;
     state = state.copyWith(showPlaylistPreviews: value);
diff --git a/lib/services/app_preferences_service.dart b/lib/services/app_preferences_service.dart
index 34581e01..9223f225 100644
--- a/lib/services/app_preferences_service.dart
+++ b/lib/services/app_preferences_service.dart
@@ -2,7 +2,6 @@ import 'package:shared_preferences/shared_preferences.dart';
 
 class AppPreferences {
   final bool animationsEnabled;
-  final bool hapticsEnabled;
   final bool showPlaylistPreviews;
   final bool showBrowseMore;
   final bool showQuickAccess;
@@ -74,7 +73,6 @@ class AppPreferences {
 
   const AppPreferences({
     this.animationsEnabled = true,
-    this.hapticsEnabled = true,
     this.showPlaylistPreviews = true,
     this.showBrowseMore = true,
     this.showQuickAccess = true,
@@ -147,7 +145,6 @@ class AppPreferences {
 
   AppPreferences copyWith({
     bool? animationsEnabled,
-    bool? hapticsEnabled,
     bool? showPlaylistPreviews,
     bool? showBrowseMore,
     bool? showQuickAccess,
@@ -219,7 +216,6 @@ class AppPreferences {
   }) {
     return AppPreferences(
       animationsEnabled: animationsEnabled ?? this.animationsEnabled,
-      hapticsEnabled: hapticsEnabled ?? this.hapticsEnabled,
       showPlaylistPreviews: showPlaylistPreviews ?? this.showPlaylistPreviews,
       showBrowseMore: showBrowseMore ?? this.showBrowseMore,
       showQuickAccess: showQuickAccess ?? this.showQuickAccess,
@@ -329,7 +325,6 @@ class AppPreferences {
 
 class AppPreferencesService {
   static const _animationsKey = 'app_animations_enabled';
-  static const _hapticsKey = 'app_haptics_enabled';
   static const _showPlaylistPreviewsKey = 'menu_show_playlist_previews';
   static const _showBrowseMoreKey = 'menu_show_browse_more';
   static const _showQuickAccessKey = 'menu_show_quick_access';
@@ -412,7 +407,6 @@ class AppPreferencesService {
     final prefs = await SharedPreferences.getInstance();
     return AppPreferences(
       animationsEnabled: prefs.getBool(_animationsKey) ?? true,
-      hapticsEnabled: prefs.getBool(_hapticsKey) ?? true,
       showPlaylistPreviews: prefs.getBool(_showPlaylistPreviewsKey) ?? true,
       showBrowseMore: prefs.getBool(_showBrowseMoreKey) ?? true,
       showQuickAccess: prefs.getBool(_showQuickAccessKey) ?? true,
@@ -526,16 +520,6 @@ class AppPreferencesService {
     await prefs.setBool(_animationsKey, value);
   }
 
-  Future<bool> getHapticsEnabled() async {
-    final prefs = await SharedPreferences.getInstance();
-    return prefs.getBool(_hapticsKey) ?? true;
-  }
-
-  Future<void> setHapticsEnabled(bool value) async {
-    final prefs = await SharedPreferences.getInstance();
-    await prefs.setBool(_hapticsKey, value);
-  }
-
   Future<bool> getShowPlaylistPreviews() async {
     final prefs = await SharedPreferences.getInstance();
     return prefs.getBool(_showPlaylistPreviewsKey) ?? true;
diff --git a/lib/widgets/common/rotary_knob.dart b/lib/widgets/common/rotary_knob.dart
index f6c8e416..566b8b1d 100644
--- a/lib/widgets/common/rotary_knob.dart
+++ b/lib/widgets/common/rotary_knob.dart
@@ -5,7 +5,6 @@ import 'package:flutter/material.dart';
 
 import 'package:flick/core/theme/adaptive_color_provider.dart';
 import 'package:flick/core/theme/app_colors.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 
 /// A circular rotary knob for parameter control.
 ///
@@ -42,7 +41,6 @@ class RotaryKnob extends StatefulWidget {
 class _RotaryKnobState extends State<RotaryKnob>
     with SingleTickerProviderStateMixin {
   double _currentValue = 0.0;
-  double _lastHapticValue = 0.0;
   double _targetValue = 0.0;
   bool _isDragging = false;
   late AnimationController _animController;
@@ -59,7 +57,6 @@ class _RotaryKnobState extends State<RotaryKnob>
     super.initState();
     _targetValue = widget.value.clamp(widget.min, widget.max).toDouble();
     _currentValue = _targetValue;
-    _lastHapticValue = _currentValue;
     _animController = AnimationController(
       vsync: this,
       duration: const Duration(milliseconds: 350),
@@ -100,7 +97,6 @@ class _RotaryKnobState extends State<RotaryKnob>
       if (_isDragging) {
         _animController.stop();
         _currentValue = newTarget;
-        _lastHapticValue = newTarget;
       } else {
         _animateTo(newTarget);
       }
@@ -167,12 +163,6 @@ class _RotaryKnobState extends State<RotaryKnob>
         (_currentValue + valueDelta).clamp(widget.min, widget.max);
     final snapped = (newValue * 10).round() / 10.0;
 
-    final diff = (snapped - _lastHapticValue).abs();
-    if (diff >= 0.5) {
-      AppHaptics.selection();
-      _lastHapticValue = snapped;
-    }
-
     setState(() => _currentValue = snapped);
     widget.onChanged!(_currentValue);
   }
diff --git a/lib/widgets/navigation/flick_nav_bar.dart b/lib/widgets/navigation/flick_nav_bar.dart
index b1215690..bcb60e89 100644
--- a/lib/widgets/navigation/flick_nav_bar.dart
+++ b/lib/widgets/navigation/flick_nav_bar.dart
@@ -1,7 +1,6 @@
 import 'package:flutter/material.dart';
 import 'package:flick/core/theme/app_colors.dart';
 import 'package:flick/core/constants/app_constants.dart';
-import 'package:flick/core/utils/app_haptics.dart';
 import 'package:flick/core/utils/responsive.dart';
 import 'package:lucide_icons_flutter/lucide_icons.dart';
 import 'package:flick/models/nav_bar_config.dart';
@@ -238,7 +237,6 @@ class _FlickNavItemState extends State<_FlickNavItem>
   void _onTapUp(TapUpDetails details) {
     _scaleController.reverse();
     FocusScope.of(context).unfocus();
-    AppHaptics.tap();
     widget.onTap();
   }
 
-- 
2.55.0

