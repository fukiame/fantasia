From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Sat, 1 Aug 2026 15:29:21 +0700
Subject: [PATCH] flutter analyze 2

Signed-off-by: fukiame <fukiame@proton.me>

diff --git a/lib/features/albums/screens/album_detail_screen.dart b/lib/features/albums/screens/album_detail_screen.dart
index 375717e89..e05a0f0a6 100644
--- a/lib/features/albums/screens/album_detail_screen.dart
+++ b/lib/features/albums/screens/album_detail_screen.dart
@@ -48,7 +48,6 @@ class _AlbumDetailScreenState extends ConsumerState<AlbumDetailScreen>
   static const Color _darkBase = Color(0xFF121212);
   static const double _backgroundBlend = 0.22;
 
-  final SongRepository _songRepository = SongRepository();
   final ColorExtractionService _colorService = ColorExtractionService();
 
   final ScrollController _scrollController = ScrollController();
@@ -81,10 +80,6 @@ class _AlbumDetailScreenState extends ConsumerState<AlbumDetailScreen>
     }
   }
 
-  String _albumKey(String albumName) {
-    return albumName;
-  }
-
   Duration get _totalDuration {
     var total = Duration.zero;
     for (final song in widget.songs) {
diff --git a/lib/features/menu/screens/menu_screen.dart b/lib/features/menu/screens/menu_screen.dart
index 99a9f8bdd..d56202785 100644
--- a/lib/features/menu/screens/menu_screen.dart
+++ b/lib/features/menu/screens/menu_screen.dart
@@ -37,9 +37,6 @@ class MenuScreen extends ConsumerStatefulWidget {
 
 class _MenuScreenState extends ConsumerState<MenuScreen>
     with TickerProviderStateMixin {
-  final PlayerService _playerService = PlayerService();
-
-  String? _heroColorArtPath;
   bool _isRefreshing = false;
   late final AnimationController _refreshController;
 
@@ -559,19 +556,6 @@ class _MenuScreenState extends ConsumerState<MenuScreen>
     final trimmed = rawArtist.trim();
     return trimmed.isEmpty ? 'Unknown Artist' : trimmed;
   }
-
-  List<Song> _dedupeSongs(Iterable<Song> songs) {
-    final seenIds = <String>{};
-    final unique = <Song>[];
-
-    for (final song in songs) {
-      if (seenIds.add(song.id)) {
-        unique.add(song);
-      }
-    }
-
-    return unique;
-  }
 }
 
 class _MenuLoadingState extends StatelessWidget {
@@ -1052,22 +1036,6 @@ class _MenuHomeData {
   });
 }
 
-class _ArtistSpotlight {
-  final String name;
-  final int plays;
-  final int uniqueSongs;
-  final String? artPath;
-  final List<Song> songs;
-
-  const _ArtistSpotlight({
-    required this.name,
-    required this.plays,
-    required this.uniqueSongs,
-    required this.artPath,
-    required this.songs,
-  });
-}
-
 class _PlaylistPreview {
   final Playlist playlist;
   final String? coverArtPath;
diff --git a/lib/features/player/widgets/animated_song_scene.dart b/lib/features/player/widgets/animated_song_scene.dart
index f4566845c..7bcd324e9 100644
--- a/lib/features/player/widgets/animated_song_scene.dart
+++ b/lib/features/player/widgets/animated_song_scene.dart
@@ -616,9 +616,6 @@ class AnimatedSongScene extends StatelessWidget {
             : context.responsive(11.0, 12.0, 13.0)) *
         artworkCardTextScale;
 
-    final diagnostics = ProviderScope.containerOf(
-      context,
-    ).read(audioOutputDiagnosticsProvider);
     final hasAlbum = song.album != null && song.album!.trim().isNotEmpty;
 
     return Column(
diff --git a/lib/features/player/widgets/bit_perfect_indicator.dart b/lib/features/player/widgets/bit_perfect_indicator.dart
index 2735ca032..46009a8ac 100644
--- a/lib/features/player/widgets/bit_perfect_indicator.dart
+++ b/lib/features/player/widgets/bit_perfect_indicator.dart
@@ -31,7 +31,6 @@ class BitPerfectIndicator extends ConsumerWidget {
   @override
   Widget build(BuildContext context, WidgetRef ref) {
     final diagnostics = ref.watch(audioOutputDiagnosticsProvider);
-    final prefs = ref.watch(appPreferencesProvider);
 
     final state = _resolveState(
       diagnostics,
@@ -246,7 +245,6 @@ class _AudioInfoBottomSheetState extends ConsumerState<_AudioInfoBottomSheet> {
 
   @override
   Widget build(BuildContext context) {
-    final prefs = ref.watch(appPreferencesProvider);
     final colorMode = ref.watch(albumColorModeProvider);
     final dominantColor = ref.watch(albumDominantColorSyncProvider);
     final Color? albumColor =
@@ -323,7 +321,6 @@ class _AudioInfoBottomSheetState extends ConsumerState<_AudioInfoBottomSheet> {
                       child: _buildPageView(
                         context,
                         albumColor: albumColor,
-                        prefs: prefs,
                         expanded: _isExpanded,
                       ),
                     ),
@@ -778,7 +775,6 @@ class _AudioInfoBottomSheetState extends ConsumerState<_AudioInfoBottomSheet> {
   Widget _buildPageView(
     BuildContext context, {
     required Color? albumColor,
-    required AppPreferences prefs,
     required bool expanded,
   }) {
     return PageView(
diff --git a/lib/features/settings/screens/app_info_settings_screen.dart b/lib/features/settings/screens/app_info_settings_screen.dart
index e7bce358e..4ce057bf2 100644
--- a/lib/features/settings/screens/app_info_settings_screen.dart
+++ b/lib/features/settings/screens/app_info_settings_screen.dart
@@ -20,21 +20,13 @@ class AppInfoSettingsScreen extends ConsumerStatefulWidget {
 class _AppInfoSettingsScreenState extends ConsumerState<AppInfoSettingsScreen>
     with SingleTickerProviderStateMixin {
 
-  late final AnimationController _donationPulseController;
-  late final Animation<double> _donationPulseAnimation;
-
   @override
   void initState() {
     super.initState();
-    _donationPulseController = AnimationController(
-      duration: const Duration(milliseconds: 2000),
-      vsync: this,
-    );
   }
 
   @override
   void dispose() {
-    _donationPulseController.dispose();
     super.dispose();
   }
 
diff --git a/lib/features/songs/screens/songs_screen.dart b/lib/features/songs/screens/songs_screen.dart
index 86edc9072..7c30d83f4 100644
--- a/lib/features/songs/screens/songs_screen.dart
+++ b/lib/features/songs/screens/songs_screen.dart
@@ -129,9 +129,6 @@ class _SongsScreenState extends ConsumerState<SongsScreen>
       NavBarButton.search,
     );
 
-    final sortOption =
-        songsAsync.value?.sortOption ?? SongSortOption.albumArtist;
-
     return Stack(
         clipBehavior: Clip.none,
         children: [
diff --git a/lib/services/album_art_import_service.dart b/lib/services/album_art_import_service.dart
index 3e4c61126..b60ed54b9 100644
--- a/lib/services/album_art_import_service.dart
+++ b/lib/services/album_art_import_service.dart
@@ -229,14 +229,6 @@ class AlbumArtImportService {
     return file;
   }
 
-  String _escapeQueryValue(String value) {
-    return value.replaceAll('"', ' ').trim();
-  }
-
-  String _normalize(String value) {
-    return value.toLowerCase().replaceAll(RegExp(r'[^a-z0-9]+'), ' ').trim();
-  }
-
   bool _looksLikeImage(Uint8List bytes) {
     if (bytes.isEmpty) {
       return false;
-- 
2.55.0

