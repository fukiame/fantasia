From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Mon, 10 Aug 2026 22:43:19 +0700
Subject: [PATCH] leftist2

Signed-off-by: fukiame <fukiame@proton.me>

diff --git a/lib/features/player/widgets/player_action_button_row.dart b/lib/features/player/widgets/player_action_button_row.dart
index 11480009..503b91d3 100644
--- a/lib/features/player/widgets/player_action_button_row.dart
+++ b/lib/features/player/widgets/player_action_button_row.dart
@@ -87,12 +87,27 @@ class _PlayerActionButtonRowState extends ConsumerState<PlayerActionButtonRow> {
           child: Row(
             mainAxisSize: MainAxisSize.min,
             children: [
-              _buildPlayerBadge(context, song.fileType.toUpperCase()),
+              BitPerfectIndicator(
+                song: song,
+                playerService: widget.playerService,
+                onTap: () {
+                  final diagnostics = ref.read(audioOutputDiagnosticsProvider);
+                  final deviceStatus = ref.read(uac2DeviceStatusProvider);
+                  BitPerfectIndicator.showInfoSheet(
+                    context,
+                    song: song,
+                    diagnostics: diagnostics,
+                    deviceStatus: deviceStatus,
+                    playerService: widget.playerService,
+                  );
+                },
+              ),
+              SizedBox(width: context.responsive(5.0, 6.0, 7.0)),
               if (song.isDsd && song.dsdRateLabel.isNotEmpty) ...[
                 SizedBox(width: context.responsive(5.0, 6.0, 7.0)),
                 Flexible(
                   child: Text(
-                    song.dsdRateLabel,
+                    song.fileType.toUpperCase() + song.dsdRateLabel,
                     maxLines: 1,
                     overflow: TextOverflow.ellipsis,
                     style: TextStyle(
@@ -106,7 +121,7 @@ class _PlayerActionButtonRowState extends ConsumerState<PlayerActionButtonRow> {
                 SizedBox(width: context.responsive(5.0, 6.0, 7.0)),
                 Flexible(
                   child: Text(
-                    song.resolution!,
+                    song.fileType.toUpperCase() + song.resolution!,
                     maxLines: 1,
                     overflow: TextOverflow.ellipsis,
                     style: TextStyle(
@@ -117,22 +132,6 @@ class _PlayerActionButtonRowState extends ConsumerState<PlayerActionButtonRow> {
                   ),
                 ),
               ],
-              SizedBox(width: context.responsive(5.0, 6.0, 7.0)),
-              BitPerfectIndicator(
-                song: song,
-                playerService: widget.playerService,
-                onTap: () {
-                  final diagnostics = ref.read(audioOutputDiagnosticsProvider);
-                  final deviceStatus = ref.read(uac2DeviceStatusProvider);
-                  BitPerfectIndicator.showInfoSheet(
-                    context,
-                    song: song,
-                    diagnostics: diagnostics,
-                    deviceStatus: deviceStatus,
-                    playerService: widget.playerService,
-                  );
-                },
-              ),
             ],
           ),
         ),
@@ -171,27 +170,6 @@ class _PlayerActionButtonRowState extends ConsumerState<PlayerActionButtonRow> {
     );
   }
 
-  Widget _buildPlayerBadge(BuildContext context, String label) {
-    return Container(
-      padding: EdgeInsets.symmetric(
-        horizontal: context.responsive(4.0, 5.0, 6.0),
-        vertical: 2,
-      ),
-      decoration: BoxDecoration(
-        color: Colors.white.withValues(alpha: 0.2),
-        borderRadius: BorderRadius.circular(3),
-      ),
-      child: Text(
-        label,
-        style: TextStyle(
-          fontFamily: 'ProductSans',
-          fontSize: context.responsive(9.0, 10.0, 11.0),
-          fontWeight: FontWeight.w600,
-          color: Colors.white,
-        ),
-      ),
-    );
-  }
   Widget _buildSideButtons({
     required PlayerActionButton topAction,
     required PlayerActionButton bottomAction,
-- 
2.55.0

