From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Sun, 26 Jul 2026 08:29:18 +0700
Subject: [PATCH] lib: settings: widgets: empty check subtitle

Signed-off-by: fukiame <fukiame@proton.me>

diff --git a/lib/features/settings/widgets/settings_widgets.dart b/lib/features/settings/widgets/settings_widgets.dart
index 4a0d37c90..bec0c19e9 100644
--- a/lib/features/settings/widgets/settings_widgets.dart
+++ b/lib/features/settings/widgets/settings_widgets.dart
@@ -133,6 +133,7 @@ class ToggleSetting extends StatelessWidget {
                         color: context.adaptiveTextPrimary,
                       ),
                     ),
+		    if ("" != subtitle) ...[
                     const SizedBox(height: 2),
                     Text(
                       subtitle,
@@ -140,6 +141,7 @@ class ToggleSetting extends StatelessWidget {
                         color: context.adaptiveTextTertiary,
                       ),
                     ),
+		    ],
                   ],
                 ),
               ),
@@ -190,6 +192,7 @@ class NavigationSetting extends StatelessWidget {
                         color: context.adaptiveTextPrimary,
                       ),
                     ),
+		    if ("" != subtitle) ...[
                     const SizedBox(height: 2),
                     Text(
                       subtitle,
@@ -197,6 +200,7 @@ class NavigationSetting extends StatelessWidget {
                         color: context.adaptiveTextTertiary,
                       ),
                     ),
+		    ],
                   ],
                 ),
               ),
@@ -253,6 +257,7 @@ class SelectionSetting extends StatelessWidget {
                         color: context.adaptiveTextPrimary,
                       ),
                     ),
+		    if ("" != subtitle) ...[
                     const SizedBox(height: 2),
                     Text(
                       subtitle,
@@ -260,6 +265,7 @@ class SelectionSetting extends StatelessWidget {
                         color: context.adaptiveTextTertiary,
                       ),
                     ),
+		    ],
                   ],
                 ),
               ),
@@ -323,6 +329,7 @@ class ActionButton extends StatelessWidget {
                                 : context.adaptiveTextTertiary,
                       ),
                     ),
+		    if ("" != subtitle) ...[
                     const SizedBox(height: 2),
                     Text(
                       subtitle,
@@ -330,6 +337,7 @@ class ActionButton extends StatelessWidget {
                         color: context.adaptiveTextTertiary,
                       ),
                     ),
+		    ],
                   ],
                 ),
               ),
@@ -450,6 +458,7 @@ class SliderSetting extends StatelessWidget {
                         color: context.adaptiveTextPrimary,
                       ),
                     ),
+		    if ("" != subtitle) ...[
                     const SizedBox(height: 2),
                     Text(
                       subtitle,
@@ -457,6 +466,7 @@ class SliderSetting extends StatelessWidget {
                         color: context.adaptiveTextTertiary,
                       ),
                     ),
+		    ],
                   ],
                 ),
               ),
-- 
2.55.0

