From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: fukiame <fukiame@proton.me>
Date: Sun, 26 Jul 2026 11:29:16 +0700
Subject: [PATCH] lib: settings: main: kill section header

Signed-off-by: fukiame <fukiame@proton.me>

diff --git a/lib/features/settings/screens/settings_screen.dart b/lib/features/settings/screens/settings_screen.dart
index 0ac25498f..90a1fc93e 100644
--- a/lib/features/settings/screens/settings_screen.dart
+++ b/lib/features/settings/screens/settings_screen.dart
@@ -56,7 +56,6 @@ class _SettingsScreenState extends State<SettingsScreen> {
                 child: Column(
                   crossAxisAlignment: CrossAxisAlignment.start,
                   children: [
-                    _buildSectionHeader(context, 'Media'),
                     SettingsCard(
                       children: [
                         _CategoryTile(
@@ -116,7 +115,6 @@ class _SettingsScreenState extends State<SettingsScreen> {
                       ],
                     ),
                     const SizedBox(height: AppConstants.spacingLg),
-                    _buildSectionHeader(context, 'System'),
                     SettingsCard(
                       children: [
                         _CategoryTile(
@@ -146,7 +144,6 @@ class _SettingsScreenState extends State<SettingsScreen> {
                       ],
                     ),
                     const SizedBox(height: AppConstants.spacingLg),
-                    _buildSectionHeader(context, 'About'),
                     SettingsCard(
                       children: [
                         _CategoryTile(
@@ -209,23 +206,6 @@ class _SettingsScreenState extends State<SettingsScreen> {
     );
   }
 
-  Widget _buildSectionHeader(BuildContext context, String title) {
-    return Padding(
-      padding: const EdgeInsets.only(
-        left: AppConstants.spacingXs,
-        bottom: AppConstants.spacingSm,
-      ),
-      child: Text(
-        title.toUpperCase(),
-        style: Theme.of(context).textTheme.labelSmall?.copyWith(
-          color: context.adaptiveTextTertiary,
-          letterSpacing: 1.2,
-          fontWeight: FontWeight.w600,
-        ),
-      ),
-    );
-  }
-
   void _navigate(BuildContext context, Widget screen) {
     NavigationHelper.pushOnRoot(
       context,
-- 
2.55.0

